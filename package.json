{
	"name": "tree2-ports",
	"private": true,
	"description": "Tooling that generates the tree2 conformance corpus from the reference implementation",
	"scripts": {
		"fixtures": "node tools/build-ref.js && node tools/gen-fixtures.js"
	},
	"devDependencies": {
		"typescript": "^6.0.3"
	}
}
